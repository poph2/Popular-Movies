<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="5dp"
    android:orientation="vertical"
    android:padding="5dp">

    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:id="@+id/movie_grid_item_imageview"
        android:src="@mipmap/ic_launcher"
        android:scaleType="centerCrop"
        android:contentDescription="@string/movie_thumb_content_description"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"/>

    <TextView
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceSmall"
        tools:text="The nifty tools attribute :D"
        android:id="@+id/movie_grid_item_textview"
        android:gravity="center"
        android:alpha="100"
        android:background="#969696"
        android:textColor="#ffffff"
        android:singleLine="true"
        android:ellipsize="end"
        android:layout_below="@+id/movie_grid_item_imageview"
        android:layout_centerHorizontal="true" />

</RelativeLayout>